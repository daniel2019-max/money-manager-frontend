import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frankydaniel.misprestamos',
  appName: 'mis-prestamos',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
